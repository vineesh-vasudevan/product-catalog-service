version: '3.4'

services:
  catalogdb:
    image: postgres

  productcatalog:
    image: ${DOCKER_REGISTRY-}productcatalog
    build:
      context: .
      dockerfile: src/ProductCatalog/Dockerfile

volumes:
  postgres_catalog:
